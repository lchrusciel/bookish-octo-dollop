const hello = (name, age) => {
    console.log(`Hello ${name}, you are ${age} years old`)
};

const user1 = 'Elon';
const user2 = 'Jeff';

const age = 40;

hello('John', 23);
hello(user1, age);
hello(user2, 23);
